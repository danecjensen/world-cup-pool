{
  "name": "2026 FIFA World Cup Pool",
  "description": "A Rails 7 + Postgres pool app for the 2026 FIFA World Cup. Players pick W/L/T for all 72 group games and fill out a full knockout bracket; admin enters results.",
  "repository": "https://github.com/danecjensen/world-cup-pool",
  "keywords": ["rails", "world-cup", "pool", "devise", "hotwire"],
  "env": {
    "RAILS_MASTER_KEY": {
      "description": "Rails master key (from config/master.key)",
      "required": true
    },
    "RAILS_LOG_TO_STDOUT": { "value": "enabled" },
    "RAILS_SERVE_STATIC_FILES": { "value": "enabled" },
    "ADMIN_EMAIL": {
      "description": "Email for initial admin account",
      "required": false
    },
    "ADMIN_PASSWORD": {
      "description": "Password for initial admin account",
      "required": false
    },
    "ADMIN_DISPLAY_NAME": {
      "description": "Display name for initial admin account",
      "value": "Commissioner",
      "required": false
    },
    "POOL_NAME": {
      "description": "Display name for this pool",
      "required": false
    },
    "POOL_TAGLINE": {
      "description": "Footer tagline for this pool",
      "required": false
    },
    "AWS_ACCESS_KEY_ID": {
      "description": "S3 access key for /feed photo & video storage. Required because Heroku's file system is ephemeral (uploads on local disk are lost on each deploy/restart). Not needed if you provision the Bucketeer add-on, which sets its own BUCKETEER_* vars.",
      "required": false
    },
    "AWS_SECRET_ACCESS_KEY": {
      "description": "S3 secret access key (pairs with AWS_ACCESS_KEY_ID).",
      "required": false
    },
    "AWS_REGION": {
      "description": "S3 bucket region, e.g. us-east-1.",
      "value": "us-east-1",
      "required": false
    },
    "AWS_BUCKET": {
      "description": "S3 bucket name for uploaded media.",
      "required": false
    }
  },
  "addons": [
    { "plan": "heroku-postgresql:essential-0" }
  ],
  "buildpacks": [
    { "url": "heroku/ruby" }
  ],
  "scripts": {
    "postdeploy": "bundle exec rails db:seed"
  }
}
